class Service {
    fun checkCarCondition(car: Car) = println(car.condition)
    fun checkBusCondition(bus: Bus) = println(bus.condition)
    fun checkBikeCondition(bike: Bike) = println(bike.condition)
}